server:
    port: 8082

spring:
    application:
        name: document-service

    datasource:
        url: ${DB_URL:jdbc:mysql://localhost:3306/document_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC}
        username: ${DB_USER:root}
        password: ${DB_PASSWORD:root@9966}

    jpa:
        hibernate:
            ddl-auto: update
        open-in-view: false

    servlet:
        multipart:
            max-file-size: 10MB
            max-request-size: 10MB

springdoc:
    swagger-ui:
        enabled: false
    api-docs:
        enabled: true

jwt:
    secret: ${JWT_SECRET:expirymate-demo-secret-key-must-be-at-least-32-bytes}

notification-service:
    url: ${NOTIFICATION_SERVICE_URL:http://NOTIFICATION-SERVICE}

app:
    storage:
        path: ${DOCUMENT_STORAGE_PATH:uploads}

eureka:
    client:
        service-url:
            defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    instance:
        prefer-ip-address: true
